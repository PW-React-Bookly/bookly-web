
// ATENCIÓN!
// First draft of the user interface for mocking purpouses

export interface UserInterface {
    id: string,
    isActive: boolean,
    firstName: string,
    lastName: string,
    email: string,
}